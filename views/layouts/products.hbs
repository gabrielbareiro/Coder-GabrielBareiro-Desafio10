<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
</head>
<body>
    <div class="container">
        {{#if listExist}}
        <div class="d-flex p-2 bd-highlight flex-wrap">
            {{#each list}}
            <div class="card " style="max-width: 18rem; margin: 1rem;">
                <img src="{{image}}" class="card-img-top" alt="{{image}}" height="100px" width="100px">
                <div class="card-body">
                    <h5 class="card-title">{{title}}</h5>
                    <p class="card-text">$ {{price}}</p>
                </div>
            </div>
            {{/each}}
        </div>
        {{else}}
        <div class="d-flex p-2 bd-highlight flex-wrap">
            <h1>No se encontraron elementos</h1>
        </div>
        {{/if}}
    </div>
</body>
</html>

